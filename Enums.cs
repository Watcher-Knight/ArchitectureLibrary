namespace ArchitectureLibrary
{
    public enum Axis { x, y, all }

    public enum SingleAxis { x, y }

    public enum Direction { left, right, up, down }

    public enum ParameterType { Trigger, Bool, Float, Integer }
}